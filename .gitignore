# Godot-generated editor and import data
.godot/

# Exported builds
builds/
exports/

# Local editor and operating-system files
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local diagnostics and throwaway output
*.log

# Wyniki gry. Każdy przejazd u każdego przepisuje te pliki, więc w repozytorium
# biły się przy każdym pushu. Leżą w projekcie, a nie w user://, bo user://
# wskazuje katalog poza folderem repozytorium — patrz scripts/score/leaderboard.gd.
# Brak plików kod znosi: lista jest wtedy pusta, rekord zerowy.
resources/data/leaderboard.json
resources/data/highscore.json
